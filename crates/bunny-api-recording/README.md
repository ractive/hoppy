# bunny-api-recording

Shared HTTP response recording helper used by all `bunny-api-*` crates in the [hoppy](https://github.com/ractive/hoppy) workspace. Records raw API responses to disk so they can be replayed in tests without network access.

See the [hoppy repository](https://github.com/ractive/hoppy) for full documentation.
