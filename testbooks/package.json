{
  "name": "hoppy-testbooks",
  "description": "E2E lifecycle tests for the hoppy CLI",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test --timeout 60000",
    "test:update": "bun test --timeout 60000 --update-snapshots",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5"
  }
}
