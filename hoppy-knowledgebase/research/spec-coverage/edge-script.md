---
title: "Spec coverage audit — edge-script"
type: research
date: 2026-05-31
tags:
  - audit
  - openapi
  - spec-coverage
---

# Spec coverage audit — `edge-script`

Generated by `hoppy-knowledgebase/scripts/run-spec-coverage-audit.sh`
(which calls `audit-spec-coverage.sh` per tuple). **This file is
overwritten on every re-audit run** — hand-authored commentary
(severity buckets, planning recommendations) belongs in a sibling
`<slug>-buckets.md` (or similar) file, not here.

# Edge script — read shape

- **Spec file:** `specs/edge-scripting.json`
- **Schema:** `EdgeScriptModel`
- **Struct file:** `crates/bunny-net-api/src/compute/types.rs`
- **Struct:** `EdgeScript`

| Metric | Count |
|---|---|
| Properties in spec schema | 17 |
| Fields in Rust struct | 17 |
| Missing from struct (gap) | 0 |
| Casing mismatches | 0 |
| In struct, missing from spec (reverse gap) | 0 |

## Missing from struct (in spec, not in struct)

_None — struct covers every spec property._

## Casing mismatches

_None._

## Reverse gap (in struct, not in spec)

_None._

# Edge script — update payload

- **Spec file:** `specs/edge-scripting.json`
- **Schema:** `UpdateEdgeScriptModel`
- **Struct file:** `crates/bunny-net-api/src/compute/types.rs`
- **Struct:** `UpdateEdgeScript`

| Metric | Count |
|---|---|
| Properties in spec schema | 2 |
| Fields in Rust struct | 3 |
| Missing from struct (gap) | 0 |
| Casing mismatches | 0 |
| In struct, missing from spec (reverse gap) | 1 |

## Missing from struct (in spec, not in struct)

_None — struct covers every spec property._

## Casing mismatches

_None._

## Reverse gap (in struct, not in spec)

These fields may be stale or renamed in the API:

- `Id`

# Edge script — create payload

- **Spec file:** `specs/edge-scripting.json`
- **Schema:** `AddEdgeScriptModel`
- **Struct file:** `crates/bunny-net-api/src/compute/types.rs`
- **Struct:** `CreateEdgeScript`

| Metric | Count |
|---|---|
| Properties in spec schema | 6 |
| Fields in Rust struct | 6 |
| Missing from struct (gap) | 0 |
| Casing mismatches | 0 |
| In struct, missing from spec (reverse gap) | 0 |

## Missing from struct (in spec, not in struct)

_None — struct covers every spec property._

## Casing mismatches

_None._

## Reverse gap (in struct, not in spec)

_None._

