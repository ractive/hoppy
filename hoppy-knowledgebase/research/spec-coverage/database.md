---
title: "Spec coverage audit — database"
type: research
date: 2026-05-31
tags:
  - audit
  - openapi
  - spec-coverage
---

# Spec coverage audit — `database`

Generated by `hoppy-knowledgebase/scripts/run-spec-coverage-audit.sh`
(which calls `audit-spec-coverage.sh` per tuple). **This file is
overwritten on every re-audit run** — hand-authored commentary
(severity buckets, planning recommendations) belongs in a sibling
`<slug>-buckets.md` (or similar) file, not here.

# Database (V2) — read shape

- **Spec file:** `specs/database.json`
- **Schema:** `Database`
- **Struct file:** `crates/bunny-net-api/src/database/types.rs`
- **Struct:** `Database`

| Metric | Count |
|---|---|
| Properties in spec schema | 13 |
| Fields in Rust struct | 13 |
| Missing from struct (gap) | 8 |
| Casing mismatches | 5 |
| In struct, missing from spec (reverse gap) | 8 |

## Missing from struct (in spec, not in struct)

- `allow_attach`
- `block_reads`
- `block_writes`
- `current_size`
- `group_id`
- `group_name`
- `is_schema`
- `size_max`

## Casing mismatches

These names match case-insensitively. Spec ⇄ Struct:

- id ⇄ Id
- name ⇄ Name
- schema ⇄ Schema
- url ⇄ Url
- version ⇄ Version

## Reverse gap (in struct, not in spec)

These fields may be stale or renamed in the API:

- `AllowAttach`
- `BlockReads`
- `BlockWrites`
- `CurrentSize`
- `GroupId`
- `GroupName`
- `IsSchema`
- `SizeMax`

# Database (V2) — create payload

- **Spec file:** `specs/database.json`
- **Schema:** `CreateDatabaseV2Payload`
- **Struct file:** `crates/bunny-net-api/src/database/types.rs`
- **Struct:** `CreateDatabasePayload`

| Metric | Count |
|---|---|
| Properties in spec schema | 4 |
| Fields in Rust struct | 2 |
| Missing from struct (gap) | 4 |
| Casing mismatches | 0 |
| In struct, missing from spec (reverse gap) | 2 |

## Missing from struct (in spec, not in struct)

- `name`
- `primary_regions`
- `replicas_regions`
- `storage_region`

## Casing mismatches

_None._

## Reverse gap (in struct, not in spec)

These fields may be stale or renamed in the API:

- `Group`
- `Slug`

# Database (V2) — update payload

- **Spec file:** `specs/database.json`
- **Schema:** `UpdateDatabaseV2Payload`
- **Struct file:** `crates/bunny-net-api/src/database/types.rs`
- **Struct:** `UpdateDatabaseGroupPayload`

| Metric | Count |
|---|---|
| Properties in spec schema | 2 |
| Fields in Rust struct | 1 |
| Missing from struct (gap) | 2 |
| Casing mismatches | 0 |
| In struct, missing from spec (reverse gap) | 1 |

## Missing from struct (in spec, not in struct)

- `primary_regions`
- `replicas_regions`

## Casing mismatches

_None._

## Reverse gap (in struct, not in spec)

These fields may be stale or renamed in the API:

- `DisplayName`

